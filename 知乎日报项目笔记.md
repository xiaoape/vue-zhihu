daily下就是知乎日报项目代码，在该目录下执行npm install命令会自动安装所有的依赖，然后执行npm run dev启动webpack服务，执行node proxy.js启动代理服务(这里好像是要另开一个CLI才行)

package-lock.json的作用
其实用一句话来概括很简单，就是锁定安装时的包的版本号，并且需要上传到git，以保证其他人在npm install时大家的依赖能保证一致。

proxy.js

知乎日报的接口地址前缀为http://news-at.zhihu.com/api/4/,图片地址前缀为https://pic1.zhimg.com,由于两者都开启了跨域限制，无法在前端直接调用，因此要开发一个代理--proxy.js文件
代理的核心是在返回的头部中添加一项Access-Control-Allow-Origin为"*",也就是允许所有的域访问。
再终端用node启动代理服务就行，浏览器将会监听两个端口

util.js
实现ajax请求，这里使用的是axios做异步请求

在daily目录下执行npm install 命令安装所有的依赖
执行npm run dev启动webpack，(这时context默认就是执行的目录daily),相当于执行
在package.json中查看到如下:"dev":"webpack-dev-server --open --history-api-fallback --config webpack.config.js"

使用 extract-text-webpack-plugin就可以把css从js中独立抽离出来
 css: ExtractTextPlugin.extract({
                            use: 'css-loader',
                            fallback: 'vue-style-loader'
                            })

把style-loader传给了fallback，这个配置的基本含义就是，extract默认行为先使用css-loader编译css，如果一切顺利的话，结束之后把css导出到规定的文件去。但是如果编译过程中出现了错误，则继续使用vue-style-loader处理css。

extract-text-webpack-plugin插件

chunks 选项的作用主要是针对多入口(entry)文件。当你有多个入口文件的时候，对应就会生成多个编译后的 js 文件。那么 chunks 选项就可以决定是否都使用这些生成的 js 文件。

chunks 默认会在生成的 html 文件中引用所有的 js 文件，当然你也可以指定引入哪些特定的文件。

注意：你启动webpack-dev-server后，你在目标文件夹中是看不到编译后的文件的,实时编译后的文件都保存到了内存当中。因此很多同学使用webpack-dev-server进行开发的时候都看不到编译后的文件

----------------------------------------------------------
在自动打开网页后，页面首先初始化mounted,
执行getRecommendList()，获取每日推荐列表
$.ajax.get('news/before/' + prevDay).then(res => {
                    this.recommendList.push(res);
                    this.isLoading = false;
                })
recommendList为推荐文章列表的数据，在初始化和每次点击“每日推荐”菜单都会请求数据。

再执行getThemes()，获取主题日报的分类列表，初始化时，这里是隐藏的，看不见
$.ajax.get('themes').then(res => {
                    this.themes = res.others;
                })
others数据为主题日报类目列表，
增加监听中栏的滚动事件，再合适的时机触发加载请求。

行为：
点击每日推荐列表的项：
触发handleClick(item.id)
handleClick (id) {
                this.articleId = id;
            }

在daily-article.vue中有
watch: {
            id (val) {
                if (val) this.getArticle();
            }
        }
监听这id的变化，一旦id发生变化后，即调用getArticle()

点击主题日报中的子类的项，过程如上
--------------------------------------------------------